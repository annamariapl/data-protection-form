  <div class="container">
  	<div class="row">
  		<div class="col-xs-12 col-sm-6 col-sm-offset-3">
  			<% @agreement.answers.each do |a| %>
  			<p><%= a.sentence %></p>
  			<% end %>
  		</div>
  	</div>
  </div>